"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import { Eye, EyeOff, LogIn } from "lucide-react";
export default function LoginPage(){const router=useRouter();const[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||'Invalid email or password');return}window.dispatchEvent(new Event('noir-auth-changed'));router.push(d.user.role==='ADMIN'?'/admin':'/account');router.refresh()}return <main className="min-h-screen bg-[radial-gradient(circle_at_top,#ead4b8,#c9986f_38%,#26140c_100%)] text-[#351d12]"><Navbar/><section className="mx-auto flex min-h-screen max-w-5xl items-center justify-center px-5 py-32"><form onSubmit={submit} className="w-full max-w-lg rounded-[42px] border border-white/30 bg-[#f7eadb]/95 p-8 shadow-[0_50px_150px_rgba(25,10,4,.45)] backdrop-blur-xl md:p-12"><div className="grid h-14 w-14 place-items-center rounded-full bg-[#3c2115] text-white"><LogIn/></div><p className="mt-6 text-[9px] uppercase tracking-[.45em] text-[#9e6745]">Welcome back</p><h1 className="mt-3 font-serif text-6xl">Log in.</h1><div className="mt-8 space-y-4"><input name="email" type="email" required placeholder="Email" className="w-full rounded-2xl bg-white/70 px-5 py-4 outline-none ring-[#8c5a3c] focus:ring-2"/><div className="relative"><input name="password" type={show?'text':'password'} required placeholder="Password" className="w-full rounded-2xl bg-white/70 px-5 py-4 pr-12 outline-none ring-[#8c5a3c] focus:ring-2"/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-4 top-1/2 -translate-y-1/2">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div>{error&&<p className="mt-4 rounded-2xl bg-red-100 p-3 text-sm text-red-800">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-full bg-[#3c2115] px-6 py-4 text-white disabled:opacity-50">{loading?'Signing in...':'Log in'}</button><p className="mt-5 text-center text-sm">New to NØIR? <a className="font-semibold underline" href="/register">Create account</a></p></form></section><Footer/></main>}
