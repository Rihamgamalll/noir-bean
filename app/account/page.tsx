"use client";
import { useEffect,useState } from "react";
import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import { User } from "lucide-react";
export default function AccountPage(){
 const [user,setUser]=useState<any>(null),[name,setName]=useState(""),[phone,setPhone]=useState(""),[msg,setMsg]=useState("");
 useEffect(()=>{fetch('/api/profile').then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(d=>{setUser(d.user);setName(d.user.name||'');setPhone(d.user.phone||'')}).catch(()=>setMsg('Please log in to manage your account.'))},[]);
 async function save(){setMsg('Saving...');const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,phone})});setMsg(r.ok?'Profile updated successfully.':'Could not update profile.');}
 return <main className="min-h-screen bg-[#ead9c3] text-[#3a2115]"><Navbar/><section className="mx-auto max-w-[900px] px-6 pb-24 pt-36"><p className="text-[10px] uppercase tracking-[.4em] text-[#9c6746]">NØIR member</p><h1 className="mt-4 font-serif text-6xl md:text-7xl">My account.</h1><div className="mt-10 rounded-[32px] bg-white/55 p-7"><User/><h2 className="mt-4 font-serif text-3xl">Profile details</h2>{user?<div className="mt-6 space-y-4"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Full name" className="w-full rounded-2xl bg-white/70 p-4 outline-none"/><input value={user.email} disabled className="w-full rounded-2xl bg-white/45 p-4 opacity-60"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone" className="w-full rounded-2xl bg-white/70 p-4 outline-none"/><button onClick={save} className="rounded-full bg-[#3a2115] px-6 py-3 text-white">Save changes</button></div>:<div className="mt-6 flex flex-wrap gap-3"><a href="/login" className="inline-block rounded-full bg-[#3a2115] px-6 py-3 text-white">Log in</a><a href="/register" className="inline-block rounded-full border border-[#3a2115]/20 px-6 py-3">Create account</a></div>}{msg&&<p className="mt-4 text-sm">{msg}</p>}</div></section><Footer/></main>}
