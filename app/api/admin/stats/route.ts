import { NextResponse } from "next/server"; import { requireAdmin,fail } from "@/lib/http"; import { db } from "@/lib/db";
export async function GET(r:Request){if(!requireAdmin(r))return fail("Unauthorized",401);const [[summary]]:any=await db.query("SELECT COUNT(*) totalOrders,COALESCE(SUM(CASE WHEN status='COMPLETED' THEN total ELSE 0 END),0) totalSales,SUM(status='NEW') newOrders,SUM(status='COMPLETED') completedOrders FROM orders");const [best]=await db.query("SELECT product_name name,SUM(quantity) quantity FROM order_items GROUP BY product_name ORDER BY quantity DESC LIMIT 5");return NextResponse.json({summary,bestSelling:best})}
