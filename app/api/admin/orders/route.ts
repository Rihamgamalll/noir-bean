import { NextResponse } from "next/server"; import { requireAdmin,fail } from "@/lib/http"; import { db } from "@/lib/db";
export async function GET(r:Request){if(!requireAdmin(r))return fail("Unauthorized",401);const [orders]:any=await db.query("SELECT id,order_number AS orderNumber,customer_name AS customerName,customer_phone AS customerPhone,notes,total,status,created_at AS createdAt FROM orders ORDER BY created_at DESC LIMIT 250");for(const o of orders){const [items]:any=await db.execute("SELECT product_name AS name,quantity,unit_price AS unitPrice,customization_json AS customization FROM order_items WHERE order_id=?",[o.id]);o.items=items.map((i:any)=>({...i,...(typeof i.customization==='string'?JSON.parse(i.customization):i.customization)}))}return NextResponse.json({orders})}
