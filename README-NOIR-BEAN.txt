NØIR BEAN — upgraded project

Added:
- Premium fixed responsive navbar branded NØIR BEAN
- Functional drink search linked to the menu section
- New scroll-pinned CoffeeAlchemy scene inspired by the final part of the reference video
- Milk rings, coffee beans, floating ice cubes and cinematic cup transition
- Full interactive drink menu with 14 drinks, category filters, favorites and detail/customization modal
- Premium footer

Preserved:
- Existing Hero animations
- Existing CoffeeStory animations, cards, modal, sack, cup and beans

Optional menu background:
Replace the radial background in components/DrinkMenu.tsx or add public/menu-bg.jpg and set it there.

Run:
npm install
npm run dev

Validation:
npm run typecheck passes.
The production build could not complete in the tool environment because the Next.js SWC binary download endpoint returned HTTP 503; this is an environment/network issue, not a TypeScript error.
