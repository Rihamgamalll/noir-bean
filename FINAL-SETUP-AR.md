# NØIR BEAN — التشغيل النهائي

## تشغيل محلي
1. انسخي `.env.example` إلى `.env.local` وعدّلي البيانات.
2. شغّلي MySQL ونفّذي `database.sql` داخل قاعدة `noir_bean`.
3. شغّلي:
   npm install
   $env:NODE_OPTIONS="--use-system-ca"
   npm run build
   npm run dev

## إنشاء حساب المالكة / الأدمن
- افتحي `/register`.
- اكتبي بيانات الحساب.
- في خانة **Admin access code** اكتبي نفس قيمة `ADMIN_PASSWORD` الموجودة في `.env.local`.
- بعد التسجيل سيتم فتح `/admin`، ويمكن أيضًا الدخول بكلمة ADMIN_PASSWORD القديمة من شاشة لوحة التحكم.

## معاينة الرابط
- صورة المشاركة موجودة في `public/social-preview.jpg` بمقاس 1200×630.
- بعد الاستضافة غيّري `NEXT_PUBLIC_SITE_URL` إلى رابط HTTPS الحقيقي ثم أعيدي البناء.
- واتساب وفيسبوك قد يحتفظان بالصورة القديمة مؤقتًا؛ تغيير الرابط أو إعادة فحصه يمسح الكاش.

## الشات
- الشات يقرأ المنتجات والأسعار والأقسام المتاحة مباشرة من MySQL.
- يدعم العربي والإنجليزي ويساعد في المنيو، الترشيحات، الأسعار، الكرواسون، الحساب، الطلبات، التوصيل، والمعلومات العامة عن الموقع.
